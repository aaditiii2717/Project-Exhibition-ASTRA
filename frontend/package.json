{
  "name": "astra-trust-layer",
  "private": true,
  "scripts": {
    "build:threeui": "esbuild src/threeui-entry.tsx --bundle --format=iife --target=es2020 --jsx=automatic --loader:.html=text --define:process.env.NODE_ENV=\"'production'\" --minify --outfile=threeui.bundle.js"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "three128": "npm:three@0.128.0"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "playwright-core": "^1.63.0"
  }
}
